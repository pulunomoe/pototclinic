{% extends "tests/base.twig" %}
{% import "macros.twig" as m %}

{% block testsActive %}
    font-bold text-red-500
{% endblock %}

{% block section %}
    {{ m.header("Delete "~result.value, [
        {link: "/tests/"~test.id, label: "Back"}
    ]) }}
    <form method="post" action="/tests/{{ test.id }}/results/{{ result.id }}/delete" class="mt-2">
        {{ m.inputConfirm("Yep") }}
    </form>
{% endblock %}